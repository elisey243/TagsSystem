# 🏷️ Tags System

Учебный проект по теме интеллектуального тегирования статей (ТИВ).  
Сервис получает статьи, подбирает к ним теги с помощью LLM и предоставляет API и простой веб-интерфейс для работы с тегами.

---

## Стек технологий

### Frontend
- React + Vite
- Bun (как среда выполнения и пакетный менеджер для фронтенда)
- CSS для стилизации интерфейса

### Backend
- Bun (runtime)
- Elysia (легковесный веб-фреймворк вместо NestJS)
- RESTful API (эндпоинты для получения тегов и статей, работы с LLM и служебных операций)
- Поддержка LLM-моделей для автоматического тегирования (например, deepseek, qwen, gpt-oss и др.)

### DevOps и инфраструктура
- Docker + Docker Compose для контейнеризации фронтенда, бэкенда и БД
- PostgreSQL как основная база данных, тома Docker для постоянного хранения данных
- Health-checks сервисов через `docker-compose.yml` (frontend, backend, db)

---

## Структура проекта

```
TagsSystem/
├── tags-system-front/     # Фронтенд (React + Vite + Bun)
├── tags-system-back/      # Бэкенд (Bun + Elysia)
├── tags-system-db/        # Конфигурация и данные PostgreSQL
├── docker-compose.yaml    # Docker Compose для всех сервисов
├── .env.example           # Пример переменных окружения (без секретов)
├── .gitignore             # Игнорируемые файлы (в т.ч. .env)
└── README.md              # Этот файл
```

---

## Переменные окружения

Используйте `.env.example` как шаблон и создайте `.env` рядом с `docker-compose.yaml`:

```env
# Groq / LLM
GROQ_TOKEN=your_groq_token_here

# Backend
BACKEND_PORT=4000

# Frontend
FRONTEND_PORT=8080

# Database
DB_USER=postgres
DB_PASSWORD=change_me_pls_7865
DB_DATABASE=tag_system
DB_HOST=postgres
DB_PORT=5432

# Prisma / ORM / подключение к БД
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_DATABASE}"
```

- Не коммитьте файл `.env` в репозиторий.
- При необходимости измените порты и URL в соответствии с вашей средой.

---

## Как развернуть через Docker

1. Клонировать репозиторий:
   ```bash
   git clone --recurse-submodules https://github.com/elisey243/TagsSystem.git
   cd TagsSystem
   ```

2. Создать `.env` на основе примера:
   ```bash
   cp .env.example .env
   # отредактируйте .env и задайте реальные значения пароля БД и URL API
   ```

3. Запустить сервисы:
   ```bash
   docker-compose up -d
   ```

4. Основные порты по умолчанию:
   - Фронтенд: `http://localhost:8181`
   - Backend API (Elysia): `http://localhost:4000`
   - PostgreSQL: порт `5432`

5. Остановить:
   ```bash
   docker-compose down
   ```

---

## Основные команды (Docker)

| Команда                      | Описание                               |
|------------------------------|----------------------------------------|
| `docker-compose up -d`       | Запуск всех сервисов в фоне            |
| `docker-compose down`        | Остановка и удаление контейнеров       |
| `docker-compose ps`          | Список запущенных контейнеров          |
| `docker-compose logs -f`     | Просмотр логов всех сервисов           |
| `docker-compose up -d --build` | Пересборка образов и запуск          |

---

## Авторы

- [@Akarin39](https://github.com/Akarin39)
- [@nik3928](https://github.com/nik3928)
- [@Nikita280104](https://github.com/Nikita280104)
- [@hutsxn](https://github.com/hutsxn) aka [@lightdeadcherry](https://github.com/lightdeadcherry)
- [@SeitaAki](https://github.com/SeitaAki)

---

## Лицензия

MIT License

---

## Поддержка

При вопросах создавайте [Issues](https://github.com/elisey243/TagsSystem/issues) в репозитории.
